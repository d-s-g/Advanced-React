import Items from '../components/Items';

// import PropTypes from 'prop-types';

const shop = props => {
    return (
        <div>
            <Items></Items>
        </div>
    );
};

shop.propTypes = {
    
};

export default shop;
